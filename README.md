# DevUtils Web

A local-first browser version of the DevUtils macOS developer toolbox. The app is built with Vite, React, and TypeScript, and is designed for GitHub Pages.

## Development

```bash
npm install
npm run dev
```

## Verification

```bash
npm run test
npm run build
```

Most tools run entirely in the browser. DNS Lookup uses explicit DNS-over-HTTPS requests.
